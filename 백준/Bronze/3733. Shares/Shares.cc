// Authored by : chjh2129
#include <bits/stdc++.h>

using namespace std;

int main(){
  cin.tie(nullptr)->sync_with_stdio(false);
  int n, s;
  while(cin >> n >> s){
    cout << s / (n+1) << '\n';
  }
}