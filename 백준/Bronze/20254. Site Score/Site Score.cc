#include <bits/stdc++.h>
using namespace std;
int main(){
    cin.tie(nullptr)->sync_with_stdio(false);
    long a, b, c, d;
    cin >> a >> b >> c >> d;
    cout << 56 * a + 24 * b + 14 * c + 6 * d;
}