#include <iostream>
using namespace std;
const char nl = '\n';
void solve(){
  int n, m;
  cin >> n >> m;
  while(m--){
    int x, y; cin >> x >> y;
  }
  cout << n - 1 << nl;
}
int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);
  int t; cin >> t;
  while(t--) solve();
}